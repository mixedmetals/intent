{
  "name": "intent-framework",
  "version": "1.0.0",
  "description": "Intent - A Schema-First, AI-Native Styling Framework",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm -r --parallel dev",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint",
    "clean": "pnpm -r clean",
    "test:ci": "pnpm -r test",
    "publish:all": "pnpm -r publish --access public --no-git-checks"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.0",
    "vitest": "^1.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "packageManager": "pnpm@8.12.0"
}
---
sidebar_position: 5
---

import { Stack, Badge } from 'intent-react'

# Badge

Small status indicators for UI elements.

## Examples

### Variants

<Stack direction="row" gap="normal" style={{ marginBottom: 24 }}>
  <Badge variant="default">Default</Badge>
  <Badge variant="primary">Primary</Badge>
  <Badge variant="success">Success</Badge>
  <Badge variant="warning">Warning</Badge>
  <Badge variant="danger">Danger</Badge>
</Stack>

```tsx
<Badge variant="default">Default</Badge>
<Badge variant="primary">Primary</Badge>
<Badge variant="success">Success</Badge>
<Badge variant="warning">Warning</Badge>
<Badge variant="danger">Danger</Badge>
```

### Sizes

<Stack direction="row" gap="normal" align="center" style={{ marginBottom: 24 }}>
  <Badge size="small">Small</Badge>
  <Badge size="medium">Medium</Badge>
  <Badge size="large">Large</Badge>
</Stack>

```tsx
<Badge size="small">Small</Badge>
<Badge size="medium">Medium</Badge>
<Badge size="large">Large</Badge>
```

### With Dots

<Stack direction="row" gap="normal" style={{ marginBottom: 24 }}>
  <Badge dot variant="success">Online</Badge>
  <Badge dot variant="warning">Away</Badge>
  <Badge dot variant="danger">Busy</Badge>
</Stack>

```tsx
<Badge dot variant="success">Online</Badge>
<Badge dot variant="warning">Away</Badge>
<Badge dot variant="danger">Busy</Badge>
```

### Pills

<Stack direction="row" gap="normal" style={{ marginBottom: 24 }}>
  <Badge pill>React</Badge>
  <Badge pill variant="primary">TypeScript</Badge>
  <Badge pill variant="success">Shipped</Badge>
</Stack>

```tsx
<Badge pill>React</Badge>
<Badge pill variant="primary">TypeScript</Badge>
<Badge pill variant="success">Shipped</Badge>
```

## Props

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `variant` | `'default'` \| `'primary'` \| `'success'` \| `'warning'` \| `'danger'` | `'default'` | Color variant |
| `size` | `'small'` \| `'medium'` \| `'large'` | `'medium'` | Badge size |
| `pill` | `boolean` | `false` | Rounded pill style |
| `dot` | `boolean` | `false` | Show status dot |

## Usage Guidelines

- **Default** - General information
- **Primary** - Featured or highlighted items
- **Success** - Completed, active, or positive states
- **Warning** - Attention needed
- **Danger** - Errors or critical status

## CSS Output

```css
.badge {
  display: inline-flex;
  align-items: center;
  gap: var(--intent-spacing-1);
  padding: var(--intent-spacing-1) var(--intent-spacing-2);
  border-radius: var(--intent-radius-md);
  font-size: var(--intent-font-size-sm);
  font-weight: 500;
}

.badge--primary {
  background: var(--intent-color-primary-alpha);
  color: var(--intent-color-primary);
}
```

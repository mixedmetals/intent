---
sidebar_position: 25
---

import { Breadcrumbs } from 'intent-react'

# Breadcrumbs

Navigation aid showing the user's location in a hierarchy.

## Example

<div style={{ marginBottom: 24 }}>
  <Breadcrumbs 
    items={[
      { label: 'Home', href: '/' },
      { label: 'Products', href: '/products' },
      { label: 'Electronics', href: '/products/electronics' },
      { label: 'Laptops' }
    ]}
  />
</div>

```tsx
import { Breadcrumbs } from 'intent-react'

<Breadcrumbs 
  items={[
    { label: 'Home', href: '/' },
    { label: 'Products', href: '/products' },
    { label: 'Electronics', href: '/products/electronics' },
    { label: 'Laptops' }  // Current page (no href)
  ]}
/>
```

## With Icons

<div style={{ marginBottom: 24 }}>
  <Breadcrumbs 
    items={[
      { label: 'Home', href: '/', icon: 'home' },
      { label: 'Settings', href: '/settings', icon: 'settings' },
      { label: 'Profile' }
    ]}
  />
</div>

```tsx
<Breadcrumbs 
  items={[
    { label: 'Home', href: '/', icon: 'home' },
    { label: 'Settings', href: '/settings', icon: 'settings' },
    { label: 'Profile' }
  ]}
/>
```

## Collapsed

When space is limited, breadcrumbs collapse:

<div style={{ marginBottom: 24 }}>
  <Breadcrumbs 
    maxItems={3}
    items={[
      { label: 'Home', href: '/' },
      { label: 'Level 1', href: '/level1' },
      { label: 'Level 2', href: '/level2' },
      { label: 'Level 3', href: '/level3' },
      { label: 'Level 4', href: '/level4' },
      { label: 'Current' }
    ]}
  />
</div>

```tsx
<Breadcrumbs 
  maxItems={3}
  items={[...]}
/>
```

## Separator Customization

```tsx
<Breadcrumbs 
  separator="›"
  items={[...]}
/>

<Breadcrumbs 
  separator="→"
  items={[...]}
/>
```

## Props

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `items` | `Array<{label: string, href?: string, icon?: string}>` | `[]` | Breadcrumb items |
| `maxItems` | `number` | - | Maximum items to show |
| `separator` | `string` \| `ReactNode` | `'/'` | Item separator |
| `onClick` | `(item: Item) => void` | - | Click handler |

## Accessibility

- Uses `nav` element with `aria-label="Breadcrumb"`
- Current page marked with `aria-current="page"`
- Separators hidden from screen readers

## CSS Output

```css
.breadcrumbs {
  display: flex;
  align-items: center;
  flex-wrap: wrap;
  gap: var(--intent-spacing-1);
}

.breadcrumbs__list {
  display: flex;
  align-items: center;
  gap: var(--intent-spacing-2);
  list-style: none;
  margin: 0;
  padding: 0;
}

.breadcrumbs__item {
  display: flex;
  align-items: center;
  gap: var(--intent-spacing-2);
}

.breadcrumbs__link {
  color: var(--intent-color-text-muted);
  text-decoration: none;
}

.breadcrumbs__link:hover {
  color: var(--intent-color-primary);
  text-decoration: underline;
}

.breadcrumbs__current {
  color: var(--intent-color-text);
  font-weight: 500;
}

.breadcrumbs__separator {
  color: var(--intent-color-text-muted);
}
```

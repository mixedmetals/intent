---
sidebar_position: 64
---
import React from 'react';

# Combobox

Autocomplete input with dropdown suggestions.

export const Combobox = ({ options, placeholder = "Search..." }) => {
  const [value, setValue] = React.useState('')
  const [isOpen, setIsOpen] = React.useState(false)
  const [selected, setSelected] = React.useState(null)
  const filteredOptions = options.filter(o => 
    o.label.toLowerCase().includes(value.toLowerCase())
  )
  
  return (
    <div style={{ position: 'relative', width: '100%', maxWidth: '300px' }}>
      <div style={{ position: 'relative' }}>
        <input
          type="text"
          value={selected ? selected.label : value}
          onChange={(e) => {
            setValue(e.target.value)
            setSelected(null)
            setIsOpen(true)
          }}
          onFocus={() => setIsOpen(true)}
          placeholder={placeholder}
          style={{
            width: '100%',
            padding: '8px 36px 8px 12px',
            border: '1px solid var(--intent-color-border)',
            borderRadius: '8px',
            fontSize: '0.875rem',
            outline: 'none',
            background: 'var(--intent-color-surface)'
          }}
        />
        <span style={{ 
          position: 'absolute', 
          right: '12px', 
          top: '50%', 
          transform: 'translateY(-50%)',
          pointerEvents: 'none'
        }}>🔽</span>
      </div>
      
      {isOpen && filteredOptions.length > 0 && (
        <div style={{
          position: 'absolute',
          top: '100%',
          left: 0,
          right: 0,
          marginTop: '4px',
          background: 'var(--intent-color-surface)',
          border: '1px solid var(--intent-color-border)',
          borderRadius: '8px',
          boxShadow: '0 10px 15px -3px rgba(0,0,0,0.1)',
          zIndex: 50,
          maxHeight: '200px',
          overflow: 'auto'
        }}>
          {filteredOptions.map((option) => (
            <button
              key={option.value}
              onClick={() => {
                setSelected(option)
                setValue(option.label)
                setIsOpen(false)
              }}
              style={{
                display: 'flex',
                alignItems: 'center',
                gap: '8px',
                width: '100%',
                padding: '10px 12px',
                textAlign: 'left',
                background: 'none',
                border: 'none',
                cursor: 'pointer',
                fontSize: '0.875rem'
              }}
              onMouseEnter={(e) => e.target.style.background = 'var(--intent-color-surface-hover)'}
              onMouseLeave={(e) => e.target.style.background = 'none'}
            >
              {option.icon && <span>{option.icon}</span>}
              <span>{option.label}</span>
            </button>
          ))}
        </div>
      )}
    </div>
  )
}

## Examples

### Basic Combobox

<div className="intent-demo">
  <Combobox 
    options={[
      { value: 'react', label: 'React', icon: '⚛️' },
      { value: 'vue', label: 'Vue', icon: '💚' },
      { value: 'angular', label: 'Angular', icon: '🅰️' },
      { value: 'svelte', label: 'Svelte', icon: '🧡' },
      { value: 'solid', label: 'SolidJS', icon: '🔵' }
    ]}
    placeholder="Select framework..."
  />
</div>

```tsx
<Combobox 
  options={[
    { value: 'react', label: 'React' },
    { value: 'vue', label: 'Vue' }
  ]}
  placeholder="Select framework..."
/>
```

## Props

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `options` | `Array<{value: string, label: string, icon?: ReactNode}>` | `[]` | Available options |
| `value` | `string` | - | Selected value |
| `placeholder` | `string` | `"Search..."` | Input placeholder |
| `disabled` | `boolean` | `false` | Disable input |
| `onChange` | `(value: string) => void` | - | Selection handler |

## CSS Output

```css
.intent-combobox {
  position: relative;
}

.intent-combobox__input {
  width: 100%;
  padding: 8px 36px 8px 12px;
  border: 1px solid var(--intent-color-border);
  border-radius: var(--intent-radius-md);
  font-size: 0.875rem;
  background: var(--intent-color-surface);
}

.intent-combobox__listbox {
  position: absolute;
  top: 100%;
  left: 0;
  right: 0;
  margin-top: 4px;
  background: var(--intent-color-surface);
  border: 1px solid var(--intent-color-border);
  border-radius: var(--intent-radius-md);
  box-shadow: 0 10px 15px -3px rgba(0, 0, 0, 0.1);
  z-index: 50;
  max-height: 200px;
  overflow: auto;
}

.intent-combobox__option {
  display: flex;
  align-items: center;
  gap: 8px;
  width: 100%;
  padding: 10px 12px;
  text-align: left;
  background: none;
  border: none;
  cursor: pointer;
  font-size: 0.875rem;
}

.intent-combobox__option:hover {
  background: var(--intent-color-surface-hover);
}
```

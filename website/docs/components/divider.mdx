---
sidebar_position: 39
---

# Divider

Visual separator between content sections.

export const Divider = ({ label, orientation = 'horizontal' }) => {
  if (orientation === 'vertical') {
    return <div style={{ width: '1px', height: '100%', background: 'var(--intent-color-border)', alignSelf: 'stretch' }} />
  }
  
  if (label) {
    return (
      <div style={{ display: 'flex', alignItems: 'center', gap: '16px', width: '100%' }}>
        <div style={{ flex: 1, height: '1px', background: 'var(--intent-color-border)' }} />
        <span style={{ fontSize: '12px', color: 'var(--intent-color-text-muted)', textTransform: 'uppercase', whiteSpace: 'nowrap' }}>{label}</span>
        <div style={{ flex: 1, height: '1px', background: 'var(--intent-color-border)' }} />
      </div>
    )
  }
  
  return <div style={{ width: '100%', height: '1px', background: 'var(--intent-color-border)' }} />
}

export const Stack = ({ children, direction = 'column', gap = 'normal' }) => (
  <div className={`intent-stack intent-stack--direction-${direction} intent-stack--gap-${gap}`}>
    {children}
  </div>
)

## Examples

### Horizontal Divider

<div className="intent-demo">
  <Stack gap="normal">
    <div>Content above</div>
    <Divider />
    <div>Content below</div>
  </Stack>
</div>

```tsx
<div>Content above</div>
<Divider />
<div>Content below</div>
```

### With Label

<div className="intent-demo">
  <Stack gap="normal">
    <button className="intent-button intent-button--importance-primary intent-button--size-medium" style={{ width: '100%' }}>Sign in with Email</button>
    <Divider label="OR" />
    <button className="intent-button intent-button--importance-secondary intent-button--size-medium" style={{ width: '100%' }}>Sign in with Google</button>
  </Stack>
</div>

```tsx
<Button>Sign in with Email</Button>
<Divider label="OR" />
<Button>Sign in with Google</Button>
```

### Vertical

<div className="intent-demo">
  <div style={{ display: 'flex', alignItems: 'center', gap: '16px', height: '60px' }}>
    <span>Left</span>
    <Divider orientation="vertical" />
    <span>Right</span>
  </div>
</div>

```tsx
<Stack direction="row">
  <Text>Left</Text>
  <Divider orientation="vertical" />
  <Text>Right</Text>
</Stack>
```

## Props

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `orientation` | `'horizontal'` \| `'vertical'` | `'horizontal'` | Divider direction |
| `label` | `string` | - | Center label text |

## CSS Output

```css
.intent-divider {
  border: 0;
  margin: 0;
}

.intent-divider--horizontal {
  width: 100%;
  border-top: 1px solid var(--intent-color-border);
}

.intent-divider--vertical {
  width: 1px;
  height: 100%;
  border-left: 1px solid var(--intent-color-border);
}

.intent-divider__label {
  font-size: 12px;
  color: var(--intent-color-text-muted);
  text-transform: uppercase;
}
```

---
sidebar_position: 26
---

import { useState } from 'react'
import { Stack, Pagination } from 'intent-react'

export function PaginationDemo() {
  const [page, setPage] = useState(1)
  
  return (
    <div style={{ marginBottom: 24 }}>
      <Pagination 
        currentPage={page}
        totalPages={10}
        onPageChange={setPage}
      />
    </div>
  )
}

# Pagination

Navigation for dividing content across multiple pages.

## Example

<PaginationDemo />

```tsx
import { useState } from 'react'
import { Pagination } from 'intent-react'

function Example() {
  const [page, setPage] = useState(1)
  
  return (
    <Pagination 
      currentPage={page}
      totalPages={10}
      onPageChange={setPage}
    />
  )
}
```

## Compact Variant

<div style={{ marginBottom: 24 }}>
  <Pagination 
    variant="compact"
    currentPage={3}
    totalPages={10}
    onPageChange={() => {}}
  />
</div>

```tsx
<Pagination 
  variant="compact"
  currentPage={3}
  totalPages={10}
  onPageChange={handlePageChange}
/>
```

## With Total Items

<div style={{ marginBottom: 24 }}>
  <Pagination 
    currentPage={1}
    totalPages={20}
    totalItems={387}
    pageSize={20}
    onPageChange={() => {}}
  />
</div>

```tsx
<Pagination 
  currentPage={1}
  totalPages={20}
  totalItems={387}
  pageSize={20}
  onPageChange={handlePageChange}
/>
```

## Sizes

<Stack direction="row" gap="normal" style={{ marginBottom: 24 }}>
  <Pagination 
    size="small"
    currentPage={5}
    totalPages={10}
    onPageChange={() => {}}
  />
  <Pagination 
    size="medium"
    currentPage={5}
    totalPages={10}
    onPageChange={() => {}}
  />
  <Pagination 
    size="large"
    currentPage={5}
    totalPages={10}
    onPageChange={() => {}}
  />
</Stack>

```tsx
<Pagination size="small" ... />
<Pagination size="medium" ... />
<Pagination size="large" ... />
```

## Edge Cases

### First Page

```tsx
<Pagination currentPage={1} totalPages={10} onPageChange={...} />
```

### Last Page

```tsx
<Pagination currentPage={10} totalPages={10} onPageChange={...} />
```

### Single Page

```tsx
<Pagination currentPage={1} totalPages={1} onPageChange={...} />
```

## Props

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `currentPage` | `number` | - | Current active page |
| `totalPages` | `number` | - | Total number of pages |
| `totalItems` | `number` | - | Total item count (for display) |
| `pageSize` | `number` | - | Items per page (for display) |
| `onPageChange` | `(page: number) => void` | - | Page change callback |
| `variant` | `'default'` \| `'compact'` | `'default'` | Pagination style |
| `size` | `'small'` \| `'medium'` \| `'large'` | `'medium'` | Pagination size |
| `showFirstLast` | `boolean` | `true` | Show first/last buttons |

## CSS Output

```css
.pagination {
  display: flex;
  align-items: center;
  gap: var(--intent-spacing-1);
}

.pagination__button {
  display: flex;
  align-items: center;
  justify-content: center;
  min-width: 2rem;
  height: 2rem;
  padding: 0 var(--intent-spacing-2);
  border-radius: var(--intent-radius-md);
  color: var(--intent-color-text);
  background: transparent;
  cursor: pointer;
}

.pagination__button:hover:not(:disabled) {
  background: var(--intent-color-surface-elevated);
}

.pagination__button--active {
  background: var(--intent-color-primary);
  color: white;
}

.pagination__button:disabled {
  opacity: 0.5;
  cursor: not-allowed;
}

.pagination__ellipsis {
  padding: 0 var(--intent-spacing-2);
  color: var(--intent-color-text-muted);
}
```

## Usage with Data

```tsx
import { useState } from 'react'
import { Pagination } from 'intent-react'

const ITEMS_PER_PAGE = 10

function ProductList({ products }) {
  const [currentPage, setCurrentPage] = useState(1)
  
  const totalPages = Math.ceil(products.length / ITEMS_PER_PAGE)
  const startIndex = (currentPage - 1) * ITEMS_PER_PAGE
  const displayedProducts = products.slice(
    startIndex, 
    startIndex + ITEMS_PER_PAGE
  )
  
  return (
    <>
      {displayedProducts.map(product => (
        <ProductCard key={product.id} product={product} />
      ))}
      
      <Pagination 
        currentPage={currentPage}
        totalPages={totalPages}
        totalItems={products.length}
        pageSize={ITEMS_PER_PAGE}
        onPageChange={setCurrentPage}
      />
    </>
  )
}
```

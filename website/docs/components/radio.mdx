---
sidebar_position: 22
---

import { useState } from 'react'
import { Stack, Radio, Field } from 'intent-react'

export function RadioDemo() {
  const [selected, setSelected] = useState('starter')
  
  return (
    <Field label="Select a plan" style={{ marginBottom: 24 }}>
      <Stack gap="normal">
        <Radio 
          name="plan"
          value="starter"
          checked={selected === 'starter'}
          onChange={(e) => setSelected(e.target.value)}
          label="Starter - $9/month"
        />
        <Radio 
          name="plan"
          value="pro"
          checked={selected === 'pro'}
          onChange={(e) => setSelected(e.target.value)}
          label="Pro - $29/month"
        />
        <Radio 
          name="plan"
          value="enterprise"
          checked={selected === 'enterprise'}
          onChange={(e) => setSelected(e.target.value)}
          label="Enterprise - $99/month"
        />
      </Stack>
    </Field>
  )
}

# Radio

Single-selection from a group of options.

## Example

<RadioDemo />

```tsx
import { useState } from 'react'
import { Radio, Stack, Field } from 'intent-react'

function Example() {
  const [plan, setPlan] = useState('starter')
  
  return (
    <Field label="Select a plan">
      <Stack gap="normal">
        <Radio 
          name="plan"
          value="starter"
          checked={plan === 'starter'}
          onChange={(e) => setPlan(e.target.value)}
          label="Starter - $9/month"
        />
        <Radio 
          name="plan"
          value="pro"
          checked={plan === 'pro'}
          onChange={(e) => setPlan(e.target.value)}
          label="Pro - $29/month"
        />
      </Stack>
    </Field>
  )
}
```

## Sizes

<Stack gap="normal" style={{ marginBottom: 24 }}>
  <Radio size="small" name="size" label="Small radio" />
  <Radio size="medium" name="size" label="Medium radio" />
  <Radio size="large" name="size" label="Large radio" />
</Stack>

```tsx
<Radio size="small" label="Small radio" />
<Radio size="medium" label="Medium radio" />
<Radio size="large" label="Large radio" />
```

### Disabled

<Stack gap="normal" style={{ marginBottom: 24 }}>
  <Radio disabled name="disabled" label="Disabled unchecked" />
  <Radio disabled checked name="disabled" label="Disabled checked" />
</Stack>

```tsx
<Radio disabled label="Disabled unchecked" />
<Radio disabled checked label="Disabled checked" />
```

### Description

<div style={{ marginBottom: 24 }}>
  <Radio 
    name="notification"
    label="Email notifications"
    description="Receive updates via email"
  />
</div>

```tsx
<Radio 
  label="Email notifications"
  description="Receive updates via email"
/>
```

## Props

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `name` | `string` | - | Radio group name |
| `value` | `string` | - | Radio value |
| `label` | `string` | - | Radio label |
| `description` | `string` | - | Helper text |
| `size` | `'small'` \| `'medium'` \| `'large'` | `'medium'` | Radio size |
| `checked` | `boolean` | - | Controlled checked state |
| `disabled` | `boolean` | `false` | Disable radio |
| `onChange` | `(e: ChangeEvent) => void` | - | Change handler |

## Radio vs Checkbox

Use **Radio** when:
- Only one option can be selected
- Selecting an option immediately shows the result
- All options should be visible

Use **Checkbox** when:
- Multiple options can be selected
- Options are independent
- Toggle a single on/off state

Use **Select** when:
- Space is limited
- Many options exist (5+)
- Options are familiar to users

## CSS Output

```css
.radio {
  display: flex;
  align-items: flex-start;
  gap: var(--intent-spacing-2);
  cursor: pointer;
}

.radio__input {
  width: 1rem;
  height: 1rem;
  border: 1px solid var(--intent-color-border);
  border-radius: 50%;
  appearance: none;
  cursor: pointer;
}

.radio__input:checked {
  border-color: var(--intent-color-primary);
  background: var(--intent-color-primary);
  box-shadow: inset 0 0 0 3px white;
}

.radio__input:disabled {
  opacity: 0.5;
  cursor: not-allowed;
}

.radio__label {
  font-weight: 500;
}

.radio__description {
  font-size: var(--intent-font-size-sm);
  color: var(--intent-color-text-muted);
}
```

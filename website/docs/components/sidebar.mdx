---
sidebar_position: 94
---
import React from 'react';

# Sidebar

Application sidebar navigation.

export const Sidebar = ({ items }) => {
  const [active, setActive] = React.useState(items[0]?.id)
  
  return (
    <div style={{ 
      width: '240px', 
      background: 'var(--intent-color-surface-elevated)', 
      borderRight: '1px solid var(--intent-color-border)',
      minHeight: '300px',
      padding: '16px'
    }}>
      <div style={{ fontWeight: 700, fontSize: '1.125rem', marginBottom: '16px', padding: '0 12px' }}>
        My App
      </div>
      <nav style={{ display: 'flex', flexDirection: 'column', gap: '2px' }}>
        {items.map((item) => (
          <a
            key={item.id}
            href={item.href}
            onClick={(e) => { e.preventDefault(); setActive(item.id); }}
            style={{
              display: 'flex',
              alignItems: 'center',
              gap: '12px',
              padding: '10px 12px',
              borderRadius: '6px',
              textDecoration: 'none',
              fontSize: '0.875rem',
              fontWeight: 500,
              color: active === item.id ? 'var(--intent-color-primary)' : 'var(--intent-color-foreground)',
              background: active === item.id ? 'var(--intent-color-primary-subtle, #dbeafe)' : 'transparent'
            }}
          >
            <span>{item.icon}</span>
            <span>{item.label}</span>
            {item.badge && (
              <span style={{ 
                marginLeft: 'auto', 
                padding: '2px 8px', 
                background: 'var(--intent-color-error, #ef4444)', 
                color: 'white', 
                borderRadius: '9999px', 
                fontSize: '0.75rem' 
              }}>
                {item.badge}
              </span>
            )}
          </a>
        ))}
      </nav>
    </div>
  )
}

## Examples

### Navigation Sidebar

<div className="intent-demo" style={{ padding: 0 }}>
  <Sidebar 
    items={[
      { id: 'dashboard', label: 'Dashboard', icon: '📊', href: '/dashboard' },
      { id: 'projects', label: 'Projects', icon: '📁', href: '/projects', badge: '3' },
      { id: 'tasks', label: 'Tasks', icon: '✓', href: '/tasks' },
      { id: 'team', label: 'Team', icon: '👥', href: '/team' },
      { id: 'settings', label: 'Settings', icon: '⚙️', href: '/settings' }
    ]}
  />
</div>

```tsx
<Sidebar 
  items={[
    { id: 'dashboard', label: 'Dashboard', icon: '📊', href: '/dashboard' },
    { id: 'projects', label: 'Projects', icon: '📁', href: '/projects', badge: '3' }
  ]}
/>
```

## Props

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `items` | `Array<{id: string, label: string, icon?: ReactNode, href: string, badge?: string}>` | `[]` | Navigation items |
| `defaultActive` | `string` | - | Default active item ID |

## CSS Output

```css
.intent-sidebar {
  width: 240px;
  background: var(--intent-color-surface-elevated);
  border-right: 1px solid var(--intent-color-border);
}

.intent-sidebar__nav {
  display: flex;
  flex-direction: column;
  gap: 2px;
  padding: 16px;
}

.intent-sidebar__link {
  display: flex;
  align-items: center;
  gap: 12px;
  padding: 10px 12px;
  border-radius: var(--intent-radius-md);
  text-decoration: none;
  font-size: 0.875rem;
  font-weight: 500;
  color: var(--intent-color-foreground);
}

.intent-sidebar__link--active {
  background: var(--intent-color-primary-subtle);
  color: var(--intent-color-primary);
}

.intent-sidebar__badge {
  margin-left: auto;
  padding: 2px 8px;
  background: var(--intent-color-error);
  color: white;
  border-radius: var(--intent-radius-full);
  font-size: 0.75rem;
}
```

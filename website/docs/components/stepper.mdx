---
sidebar_position: 92
---
import React from 'react';

# Stepper

Multi-step process indicator.

export const Stepper = ({ steps, currentStep = 0 }) => (
  <div style={{ display: 'flex', alignItems: 'center', width: '100%' }}>
    {steps.map((step, i) => {
      const isActive = i === currentStep
      const isCompleted = i < currentStep
      
      return (
        <React.Fragment key={i}>
          <div style={{ display: 'flex', flexDirection: 'column', alignItems: 'center', flex: i === steps.length - 1 ? '0 0 auto' : 1 }}>
            <div style={{
              width: '32px',
              height: '32px',
              borderRadius: '50%',
              display: 'flex',
              alignItems: 'center',
              justifyContent: 'center',
              fontSize: '0.875rem',
              fontWeight: 600,
              background: isCompleted ? 'var(--intent-color-success, #22c55e)' : isActive ? 'var(--intent-color-primary)' : 'var(--intent-color-surface-elevated)',
              color: isCompleted || isActive ? 'white' : 'var(--intent-color-muted)',
              border: `2px solid ${isCompleted ? 'var(--intent-color-success, #22c55e)' : isActive ? 'var(--intent-color-primary)' : 'var(--intent-color-border)'}`,
              transition: 'all 200ms'
            }}>
              {isCompleted ? '✓' : i + 1}
            </div>
            <div style={{
              marginTop: '8px',
              fontSize: '0.75rem',
              fontWeight: 500,
              color: isActive ? 'var(--intent-color-primary)' : 'var(--intent-color-muted)',
              textAlign: 'center'
            }}>
              {step}
            </div>
          </div>
          {i < steps.length - 1 && (
            <div style={{
              flex: 1,
              height: '2px',
              background: isCompleted ? 'var(--intent-color-success, #22c55e)' : 'var(--intent-color-border)',
              margin: '0 8px',
              marginBottom: '20px'
            }} />
          )}
        </React.Fragment>
      )
    })}
  </div>
)

## Examples

### Basic Stepper

<div className="intent-demo">
  <Stepper 
    steps={['Account', 'Personal', 'Billing', 'Review']}
    currentStep={1}
  />
</div>

```tsx
<Stepper 
  steps={['Account', 'Personal', 'Billing', 'Review']}
  currentStep={1}
/>
```

### Completed Steps

<div className="intent-demo">
  <Stepper 
    steps={['Cart', 'Shipping', 'Payment', 'Confirm']}
    currentStep={3}
  />
</div>

```tsx
<Stepper 
  steps={['Cart', 'Shipping', 'Payment', 'Confirm']}
  currentStep={3}
/>
```

## Props

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `steps` | `string[]` | `[]` | Step labels |
| `currentStep` | `number` | `0` | Active step index |
| `orientation` | `'horizontal' \| 'vertical'` | `'horizontal'` | Stepper orientation |

## CSS Output

```css
.intent-stepper {
  display: flex;
  align-items: center;
  width: 100%;
}

.intent-stepper__step {
  display: flex;
  flex-direction: column;
  align-items: center;
}

.intent-stepper__indicator {
  width: 32px;
  height: 32px;
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 0.875rem;
  font-weight: 600;
  border: 2px solid var(--intent-color-border);
  transition: all 200ms;
}

.intent-stepper__indicator--active {
  background: var(--intent-color-primary);
  border-color: var(--intent-color-primary);
  color: white;
}

.intent-stepper__indicator--completed {
  background: var(--intent-color-success);
  border-color: var(--intent-color-success);
  color: white;
}

.intent-stepper__label {
  margin-top: 8px;
  font-size: 0.75rem;
  font-weight: 500;
}

.intent-stepper__connector {
  flex: 1;
  height: 2px;
  background: var(--intent-color-border);
  margin: 0 8px;
}

.intent-stepper__connector--completed {
  background: var(--intent-color-success);
}
```

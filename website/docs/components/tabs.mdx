---
sidebar_position: 18
---

import React from 'react';

# Tabs

Organize content into tabbed sections.

export const TabsDemo = ({ variant }) => {
  const [activeTab, setActiveTab] = React.useState('account')
  
  const tabs = [
    { id: 'account', label: 'Account', content: 'Manage your account settings and preferences.' },
    { id: 'password', label: 'Password', content: 'Change your password and security settings.' },
    { id: 'notifications', label: 'Notifications', content: 'Configure how you receive notifications.' }
  ]
  
  return (
    <div className={`intent-tabs ${variant === 'line' ? 'intent-tabs--line' : ''}`}>
      <div className="intent-tabs__list">
        {tabs.map(tab => (
          <button
            key={tab.id}
            onClick={() => setActiveTab(tab.id)}
            className={`intent-tabs__tab ${activeTab === tab.id ? 'intent-tabs__tab--active' : ''}`}
          >
            {tab.label}
          </button>
        ))}
      </div>
      <div className="intent-tabs__panel">
        {tabs.find(t => t.id === activeTab)?.content}
      </div>
    </div>
  )
}

## Examples

### Pill Tabs

<div className="intent-demo">
  <TabsDemo />
</div>

```tsx
const [activeTab, setActiveTab] = useState('account')

<Tabs value={activeTab} onChange={setActiveTab}>
  <Tabs.List>
    <Tabs.Trigger value="account">Account</Tabs.Trigger>
    <Tabs.Trigger value="password">Password</Tabs.Trigger>
  </Tabs.List>
  <Tabs.Content value="account">
    <Text>Account settings...</Text>
  </Tabs.Content>
</Tabs>
```

### Line Tabs

<div className="intent-demo">
  <TabsDemo variant="line" />
</div>

```tsx
<Tabs variant="line" value={activeTab} onChange={setActiveTab}>
  <Tabs.List>
    <Tabs.Trigger value="account">Account</Tabs.Trigger>
    <Tabs.Trigger value="password">Password</Tabs.Trigger>
  </Tabs.List>
</Tabs>
```

## Props

### Tabs
| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `defaultValue` | `string` | - | Default active tab |
| `value` | `string` | - | Controlled active tab |
| `onChange` | `(value: string) => void` | - | Tab change handler |
| `variant` | `'pill' \| 'line'` | `'pill'` | Tab style variant |

### Tabs.Trigger
| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `value` | `string` | required | Tab value |
| `children` | `ReactNode` | - | Tab label |

### Tabs.Content
| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `value` | `string` | required | Associated tab value |
| `children` | `ReactNode` | - | Tab content |

## CSS Output

```css
.intent-tabs__list {
  display: flex;
  gap: 2px;
  padding: 3px;
  background: var(--intent-surface-flat-bg);
  border: 0.5px solid var(--intent-surface-flat-border);
  border-radius: var(--intent-radius-lg);
}

.intent-tabs__tab {
  flex: 1;
  padding: 8px 16px;
  background: transparent;
  border: none;
  border-radius: calc(var(--intent-radius-lg) - 3px);
  cursor: pointer;
  transition: all 100ms cubic-bezier(0.2, 0, 0, 1);
}

.intent-tabs__tab--active {
  background: var(--intent-surface-milled-bg);
  box-shadow: var(--intent-surface-milled-shadow);
  color: oklch(95% 0 0);
}
```

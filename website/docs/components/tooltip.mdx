---
sidebar_position: 17
---

import React from 'react';

# Tooltip

Hover-activated information popup.

export const Tooltip = ({ content, position = 'top', children }) => {
  const [isVisible, setIsVisible] = React.useState(false)
  
  const getPositionStyles = () => {
    switch(position) {
      case 'top': return { bottom: '100%', left: '50%', transform: 'translateX(-50%)', marginBottom: '8px' }
      case 'bottom': return { top: '100%', left: '50%', transform: 'translateX(-50%)', marginTop: '8px' }
      case 'left': return { right: '100%', top: '50%', transform: 'translateY(-50%)', marginRight: '8px' }
      case 'right': return { left: '100%', top: '50%', transform: 'translateY(-50%)', marginLeft: '8px' }
      default: return { bottom: '100%', left: '50%', transform: 'translateX(-50%)', marginBottom: '8px' }
    }
  }
  
  return (
    <div 
      style={{ position: 'relative', display: 'inline-block' }}
      onMouseEnter={() => setIsVisible(true)}
      onMouseLeave={() => setIsVisible(false)}
    >
      {children}
      {isVisible && (
        <div style={{
          position: 'absolute',
          padding: '8px 12px',
          background: 'var(--intent-color-foreground)',
          color: 'var(--intent-color-background)',
          fontSize: '0.875rem',
          borderRadius: '6px',
          whiteSpace: 'nowrap',
          zIndex: 50,
          pointerEvents: 'none',
          ...getPositionStyles()
        }}>
          {content}
        </div>
      )}
    </div>
  )
}

## Examples

### Basic Tooltip

Hover over the button:

<div className="intent-demo">
  <Tooltip content="Click to save your changes">
    <button className="intent-btn intent-btn--variant-primary intent-btn--size-medium">Hover Me</button>
  </Tooltip>
</div>

```tsx
<Tooltip content="Click to save your changes">
  <Button>Hover Me</Button>
</Tooltip>
```

### Positions

<div className="intent-demo">
  <div style={{ display: 'flex', gap: '16px', justifyContent: 'center' }}>
    <Tooltip content="Top position" position="top">
      <button className="intent-btn intent-btn--variant-secondary intent-btn--size-small">Top</button>
    </Tooltip>
    <Tooltip content="Right position" position="right">
      <button className="intent-btn intent-btn--variant-secondary intent-btn--size-small">Right</button>
    </Tooltip>
    <Tooltip content="Bottom position" position="bottom">
      <button className="intent-btn intent-btn--variant-secondary intent-btn--size-small">Bottom</button>
    </Tooltip>
    <Tooltip content="Left position" position="left">
      <button className="intent-btn intent-btn--variant-secondary intent-btn--size-small">Left</button>
    </Tooltip>
  </div>
</div>

```tsx
<Tooltip content="Top" position="top">...</Tooltip>
<Tooltip content="Right" position="right">...</Tooltip>
<Tooltip content="Bottom" position="bottom">...</Tooltip>
<Tooltip content="Left" position="left">...</Tooltip>
```

### With Icons

<div className="intent-demo">
  <div style={{ display: 'flex', gap: '16px' }}>
    <Tooltip content="Add to favorites">
      <button className="intent-btn intent-btn--variant-secondary intent-btn--size-medium">❤️ Like</button>
    </Tooltip>
    <Tooltip content="Share this page">
      <button className="intent-btn intent-btn--variant-secondary intent-btn--size-medium">📤 Share</button>
    </Tooltip>
  </div>
</div>

```tsx
<Tooltip content="Add to favorites">
  <Button icon="heart">Like</Button>
</Tooltip>
```

## Props

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `content` | `string` | - | Tooltip text |
| `position` | `'top' \| 'bottom' \| 'left' \| 'right'` | `'top'` | Tooltip position |
| `children` | `ReactNode` | - | Trigger element |

## CSS Output

```css
.intent-tooltip {
  position: relative;
  display: inline-block;
}

.intent-tooltip__content {
  position: absolute;
  padding: 8px 12px;
  background: var(--intent-color-foreground);
  color: var(--intent-color-background);
  font-size: 0.875rem;
  border-radius: var(--intent-radius-md);
  white-space: nowrap;
  z-index: 50;
}
```
